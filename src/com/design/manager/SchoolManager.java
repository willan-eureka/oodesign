package com.design.manager;

import com.design.model.School;

/**
 * Specific interface for access to School entity,
 * we also have define own methods for this specific interface
 */

public interface SchoolManager extends GenericManager<School, Long> {
}
